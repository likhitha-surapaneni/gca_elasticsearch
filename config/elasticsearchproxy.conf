{
    elasticsearch_host => 'ves-hx-e4.ebi.ac.uk',
    elasticsearch_port => 9200,

   # A hash of the api base route, and the corresponding elasticsearch base route
    api_routes => {
        '/api' => '/',
        '/lines/api' => '/hipsci',
    },

    static_directories => [
            {dir => '/Users/streeter/git_repositories/gca_elasticsearch/hipsci_www/', no_cache => 1, trailing_slash => 1, auto_index => 0},
        ],

    'cors.enabled' => 1,

    hypnotoad => {
        # accepts => 1000,
        # backlog => 128,
        # clients => 1000,
        # graceful_timeout => 20,
        # group => 'vg-pub',
        # heartbeat_interval => 5,
        # heartbeat_timeout => 20,
        # inactivity_timeout => 15,
        # keep_alive_requests => 25,
         listen => ['http://*:8000'],
        # multi_accept => 50,
        # pid_file => app->home->rel_file('elasticsearchproxy.hypnotoad.pid'),
        pid_file => '/Users/streeter/git_repositories/gca_elasticsearch/home/var/run/elasticsearchproxy.hypnotoad.pid'
        # proxy => 0,
        # upgrade_timeout => 60,
        # user => 'w3-vg02',
        # workers => 4,
    },
}
