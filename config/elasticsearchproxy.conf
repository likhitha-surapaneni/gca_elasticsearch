{
    default_format => 'json',
    elasticsearch_host => 'localhost',
    elasticsearch_port => 9200,
    elasticsearch_test_path => '/_plugin/testpage/',

    elasticsearch_webabpp_index_map => {
        lines => 'hipsci',
    },

    'cors.enabled' => 1,

    hypnotoad => {
        # accepts => 1000,
        # backlog => 128,
        # clients => 1000,
        # graceful_timeout => 20,
        # group => 'vg-pub',
        # heartbeat_interval => 5,
        # heartbeat_timeout => 20,
        # inactivity_timeout => 15,
        # keep_alive_requests => 25,
         listen => ['http://*:8000'],
        # multi_accept => 50,
        # pid_file => app->home->rel_file('elasticsearchproxy.hypnotoad.pid'),
        pid_file => '/nfs/nobackup/reseq-info/web-dev/server_homes/vg-rs-dev1.ebi.ac.uk/var/run/elasticsearchproxy.hypnotoad.pid',
        # proxy => 0,
        # upgrade_timeout => 60,
        # user => 'w3-vg02',
        # workers => 4,
    },
}
